extends layout

block content

    .body
        .container
            nav.navbar                 
                a.link(href='/adminAeropuertos')
                    button.button Administrar Aeropuertos
                a.link(href='/verCaracteristicasAeropuertos')
                    button.button Características Aeropuertos
                a.link(href='/')
                    button.button1 Home
            hr
            table.table.bg-white
                thead
                    tr 
                        th(scope='col') Código IATA
                        th(scope='col') Pista de Aterrizaje  (Km)

                tbody
                - for (var i = 0; i < caracteristicas.length; i++)
                    tr
                        td #{caracteristicas[i].Codigo_IATA}
                        td #{caracteristicas[i].Pista}